version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - DISCORD_TOKEN=your_token_here
      - TWITCH_CHANNEL=your_twitch_username
      - DB_PATH=/app/data/cozy.db
      
